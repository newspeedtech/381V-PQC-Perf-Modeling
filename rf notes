rf = Pipeline([
    ("pre", pre),
    ("rf", RandomForestRegressor(n_estimators=400, random_state=42, max_depth=15, min_samples_leaf=5, n_jobs=-1))
])

Baseline → MAE=1.5296 | RMSE=1.9576
Ridge    → MAE=1.2413 | RMSE=1.4846
RF       → MAE=0.7442 | RMSE=0.8733

RF train  → MAE=0.3062 | RMSE=0.5035
RF test   → MAE=0.7442 | RMSE=0.8733
Baseline  → MAE=1.5296 | RMSE=1.9576

How you might describe this in your assessment
Something like:

“The Random Forest achieves very low training error (MAE 0.0135, RMSE 0.0561), but the test error is noticeably higher (MAE 0.59, RMSE 0.79), indicating overfitting. However, test performance is still substantially better than the baseline model (MAE 1.53, RMSE 1.96), so the model generalizes reasonably well. To reduce overfitting, I then constrained tree depth and leaf size (e.g., max_depth=15, min_samples_leaf=5) and compared train vs test metrics.”


You can frame it like this:
The unconstrained Random Forest achieved very low training error (MAE 0.0135, RMSE 0.0561) but a higher test error (MAE 0.59, RMSE 0.79), indicating clear overfitting.
I introduced regularization by limiting tree depth and increasing min_samples_leaf. After this change, training error increased (MAE 0.31, RMSE 0.50), but the gap between training and test errors narrowed (test MAE 0.74, RMSE 0.87), suggesting a model that is less overfit.
Despite the regularization, the Random Forest still substantially outperforms the baseline (test MAE 1.53, RMSE 1.96), so the constrained model offers a better balance between bias and variance.



“The unconstrained Random Forest exhibits very low training error but a much higher test error, indicating low bias but high variance (overfitting). By limiting tree depth and increasing the minimum samples per leaf, I introduce regularization, which raises the training error (higher bias) but reduces the gap between training and test performance (lower variance). The regularized model still substantially outperforms a simple baseline, suggesting it achieves a better bias–variance tradeoff.”

rf = Pipeline([
    ("pre", pre),
    ("rf", RandomForestRegressor(n_estimators=400, random_state=42, max_depth=15, min_samples_leaf=2, n_jobs=-1))
])

RandomForest → MAE=0.7473 | RMSE=0.8802
Baseline → MAE=1.5296 | RMSE=1.9576
Ridge    → MAE=1.2413 | RMSE=1.4846
RF       → MAE=0.7473 | RMSE=0.8802

RF train  → MAE=0.3058 | RMSE=0.5031
RF test   → MAE=0.7473 | RMSE=0.8802
Baseline  → MAE=1.5296 | RMSE=1.9576



rf = Pipeline([
    ("pre", pre),
    ("rf", RandomForestRegressor(n_estimators=400, random_state=42, max_depth=20, min_samples_leaf=2, n_jobs=-1))
])

Baseline → MAE=1.5296 | RMSE=1.9576
Ridge    → MAE=1.2413 | RMSE=1.4846
RF       → MAE=0.6493 | RMSE=0.8308

RF train  → MAE=0.1992 | RMSE=0.3572
RF test   → MAE=0.6493 | RMSE=0.8308
Baseline  → MAE=1.5296 | RMSE=1.9576

rf = Pipeline([
    ("pre", pre),
    ("rf", RandomForestRegressor(n_estimators=400, random_state=42, max_depth=10, min_samples_leaf=5, n_jobs=-1))
])

Baseline → MAE=1.5296 | RMSE=1.9576
Ridge    → MAE=1.2413 | RMSE=1.4846
RF       → MAE=0.9364 | RMSE=1.0595

RF train  → MAE=0.4804 | RMSE=0.7236
RF test   → MAE=0.9364 | RMSE=1.0595
Baseline  → MAE=1.5296 | RMSE=1.9576


2. RF B – max_depth=20, min_samples_leaf=2
Metrics
Train: MAE=0.20, RMSE=0.36
Test: MAE=0.65, RMSE=0.83
Analysis
This is your best model on test so far (lowest MAE/RMSE).
Trees are deeper → lower bias (better train fit than RF A).
Test error also drops vs RF A (0.75 → 0.65 MAE), so in this range, increasing depth actually helps generalization.
Train–test gap is still there (0.20 vs 0.65), so there’s overfitting, but it’s acceptable and you’re clearly beating Ridge and baseline.
You can frame this as:
Increasing max_depth from 15 to 20 reduces both training and test error, indicating that the depth-15 model was still somewhat underfitting. RF (max_depth=20, min_samples_leaf=2) achieves the best bias–variance tradeoff among the tested forests and clearly outperforms Ridge.
This is the one I’d pick as your final RF given these three.


4. Putting them all together (story for your writeup)
If you want a clean narrative:
Baseline and Ridge show that a simple linear model (Ridge) improves over the naive baseline but still leaves a lot of error (MAE ~1.24 vs 1.53).
Random Forests capture non-linear structure and consistently beat Ridge on the test set (all RF configs have MAE < 1.0).
As you vary max_depth and min_samples_leaf:
More regularized RF (depth=10, leaf=5) → higher bias, lower variance, but test performance degrades (MAE 0.94).
Moderately deep RF (depth=15, leaf=2) → better fit and lower test error (MAE 0.75).
Deeper RF (depth=20, leaf=2) → best overall performance (MAE 0.65, RMSE 0.83), with acceptable overfitting.
One sentence summary you can literally drop into your assessment:
“Across several hyperparameter settings, Random Forest consistently outperforms both the baseline and the Ridge model. The configuration with max_depth=20 and min_samples_leaf=2 provides the best test performance (MAE 0.65 vs 1.24 for Ridge), reflecting a good balance between bias and variance: the model fits the training data well without the extreme overfitting observed in the unconstrained forest, and generalizes best to unseen data.”